import { styled } from '@linaria/react';

const StyledButton = styled.button`
  color: red;
`;

const App = () => (
  <div>
    text
    <StyledButton>Button</StyledButton>
  </div>
);

export default App;
